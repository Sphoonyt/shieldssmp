- name: Build with Maven
        run: mvn clean package -B
        working-directory: ShieldsSMP-plugin

      - name: Upload jar
        uses: actions/upload-artifact@v4
        with:
          name: ShieldsSMP-jar
          path: ShieldsSMP-plugin/target/ShieldsSMP-*.jar
