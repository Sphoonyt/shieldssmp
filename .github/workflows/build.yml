- name: Build with Maven
        run: mvn clean package -B
        working-directory: ShieldsSMP

      - name: Upload jar
        uses: actions/upload-artifact@v4
        with:
          name: ShieldsSMP-jar
          path: ShieldsSMP/target/ShieldsSMP-*.jar
